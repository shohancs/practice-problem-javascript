// Create an array for all the mobile phones. Display all the elements of the array by using a while loop

var phones = ['Apple', 'Samsung', 'Nokia', 'Lava', 'Blackberry'];
var i = 0;
while(i < phones.length) {
    var phone = phones[i];
    console.log(phone);
    i++;
}
